<Page
    x:Class="Decryptor.WinUI.Pages.SettingsPages.BCryptPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Decryptor.WinUI.Pages.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BCrypt Settings"
                   Style="{StaticResource SettingsTitle}"
                   Grid.ColumnSpan="2"/>

        <TextBlock Text="Work Factor"
                   Style="{StaticResource Label}"
                   Grid.Row="1"/>
        <NumberBox Value="{x:Bind ViewModel.BCryptWorkFactor, Mode=TwoWay}"
                   LargeChange="10"
                   SmallChange="1"
                   SpinButtonPlacementMode="Inline"
                   Grid.Column="1"
                   Grid.Row="1"/>
    </Grid>
</Page>
